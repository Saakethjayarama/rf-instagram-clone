import { auth } from "./Firebase";
import {
  createUser,
  signIn,
  signOut,
  updateProfile,
  checkUserNames,
} from "./Authentication";

export { createUser, signIn, signOut, updateProfile, checkUserNames, auth };
